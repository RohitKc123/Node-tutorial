// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minumum)
const sayHi = require('./4-mod-expo')
const data = require('./5-name')
require('./7-mind-grenade.js')

// sayHi('susan')
// sayHi(data.rohit)
// sayHi(data.john)